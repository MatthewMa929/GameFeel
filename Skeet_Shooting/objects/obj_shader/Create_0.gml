application_surface_draw_enable(false); //disabling automatic redrawing of the application surface
display_set_gui_size(800, 800); //making sure the GUI layer stays always the same size

ballTimestep = 0.0;
ballTime = 0.0;